import { IFinalInput } from './types';
export declare const getRequestConfig: (destination: string, input: IFinalInput, extraData?: any) => any;
export declare const isRequestBlocked: (statusCode: number, body: any) => any;
